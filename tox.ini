[tox]
envlist = py36
requires = tox-pipenv
           pytest
           pytest-mock
           pytest-structlog
sitepackages=true

[testenv]
whitelist_externals = python
commands = python -m pytest --basetemp={envtmpdir} {posargs}

[pytest]
testpaths = tests
markers = integration

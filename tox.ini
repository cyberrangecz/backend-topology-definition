[tox]
envlist = py3{8, 12}
requires = pytest
           pytest-mock
           pytest-structlog
sitepackages=true

[testenv]
whitelist_externals = python
commands = python -m pytest --basetemp={envtmpdir} {posargs}

[pytest]
testpaths = tests
markers = integration
